---
# Test 서버 전용 변수

server_name: "test-server"
domain: "test.example.com"  # CHANGE THIS

# Traefik 설정 (민감 정보는 vault.yml에서 참조)
traefik_acme_email: "{{ vault_traefik_acme_email }}"
traefik_dashboard_domain: "{{ domain }}"

# 테스트 환경 특화 설정
traefik_log_level: "DEBUG"

# 서비스별 설정 추가 필요
