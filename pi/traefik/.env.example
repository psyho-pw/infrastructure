# Domain
DOMAIN=example.com

# Let's Encrypt 이메일
ACME_EMAIL=admin@example.com

# Traefik 대시보드 인증 (htpasswd 형식)
# 생성: echo $(htpasswd -nb admin password) | sed -e s/\\$/\\$\\$/g
DASHBOARD_AUTH=admin:$$apr1$$xyz...
